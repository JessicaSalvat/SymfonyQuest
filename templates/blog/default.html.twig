{% extends 'base.html.twig' %}


{% block body %}

    <h1>Welcome sur mon blog</h1>

    <table class="table table-hover">
        <thead class="thead-dark">
        <tr>
            <th scope="col">Test my different methods</th>
        </tr>
        </thead>
        <tbody>
        <tr>
            <td><a href="{{ path('blog_show', { 'slug': "javascript-vs-php" }) }}">
                    Testing show() method from BlogController with a real article s slug.
                </a></td>
        </tr>
        <tr>
            <td>
                <a href="{{ path('blog_index') }}">
                    Testing index() method from BlogController to view all articles.
                </a></td>
        </tr>
        <tr><td> <a href="{{ path('show_category', {'name': "javascript"}) }}">
                    Testing showByCategory() method from BlogController to view all articles.
                </a></td></tr>
        <tr> <td> <a href="{{ path('tag_show', {'name': "development"}) }}">
                    Testing showTag() method from BlogController to view all tags.
                </a></td></tr>
        <tr>  <td><a href="{{ path('category_index') }}">
                    To add a new Category
                </a></td></tr>
        <tr> <td> <a href="{{ path('article_index') }}">
                    To edit an article
                </a></td></tr>

        </tbody>
    </table>

{% endblock %}